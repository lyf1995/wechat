<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.com.lyf.wechat.dao.ShareUserDao">

    <select id="selectAddressById" resultType="cn.com.lyf.wechat.entity.ShareUser">
        select * from t_address where id = #{addressId} and is_delete = '0'
    </select>

    <select id="selectAllAddress" resultType="cn.com.lyf.wechat.entity.ShareUser">
        select * from t_address t where is_delete = '0'
        <if test="userId!=null and userId!=''">
            AND t.user_id = #{userId}
        </if>
    </select>

    <select id="selectDefaultAddressByUserId" resultType="cn.com.lyf.wechat.entity.ShareUser">
        select * from t_address where user_id = #{userId} and is_delete = 0 and is_default = 1
    </select>

    <update id="updateAddress">
        UPDATE t_address SET contacts = #{address.contacts},phone = #{address.phone},province = #{address.province},city = #{address.city},area = #{address.area},detail_address = #{address.detailAddress} WHERE id = #{address.id}
    </update>

    <update id="deleteAddress">
        UPDATE t_address SET is_delete = 1 WHERE id = #{id}
    </update>

    <insert id="addShareUser">
        INSERT INTO t_share_user (share_id,click_user_id,status,amount)
        VALUES
        (#{shareUser.shareId},#{shareUser.clickUserId},#{shareUser.status},#{shareUser.amount})
    </insert>

</mapper>